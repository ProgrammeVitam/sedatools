package fr.gouv.vitam.tools.sedalib.metadata.namedtype;

public class RuleMetadataKind extends ComplexListMetadataKind {


    /**
     * Instantiates a new metadata item.
     *
     * @param metadataClass the metadata class
     * @param many the many
     */
    public RuleMetadataKind(Class<?> metadataClass, boolean many) {
        super(metadataClass, many);
    }
}
